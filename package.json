{
  "name": "council-mcp-server",
  "version": "1.0.0",
  "description": "MCP server exposing the Council Gateway API's meetings, documents and decisions endpoints as tools",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.0.0"
  }
}
